using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraSave : MonoBehaviour
{
    /*
    public static Camera CameraInstance = null;

    private void Awake()
    {
        if (CameraInstance == null)
        {
            CameraInstance = this;
        }
        else if (CameraInstance != this)
        {
            Destroy(CameraInstance);
        }
        DontDestroyOnLoad(CameraInstance);
    }   */
    void Update()
    {
        
    }
}
